<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record model="ir.actions.server" id="action_account_print_checks">
            <field name="name">Create maintenance request</field>
            <field name="model_id" ref="property_lease_management.model_assets_accessrz"/>
            <field name="binding_model_id" ref="property_lease_management.model_assets_accessrz" />
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
        if records:
            action = records.create_maintenance_request()
            </field>
        </record>

        <record model="ir.actions.server" id="action_unit_send_mail">
            <field name="name">Send Mail to Tenant</field>
            <field name="model_id" ref="property_lease_management.model_property_property"/>
            <field name="binding_model_id" ref="property_lease_management.model_property_property"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
                if records:
                action = records.send_unit_mail()
            </field>
        </record>
    </data>
</odoo>
