<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="action_mass_confirm" model="ir.actions.server">
            <field name="name">Mass Confirm</field>
            <field name="model_id" ref="aqar_documents.model_document_renewal"/>
            <field name="binding_model_id" ref="aqar_documents.model_document_renewal"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
                if records:
                action = records.mass_confirm()
            </field>
        </record>
</odoo>