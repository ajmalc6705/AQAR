<?xml version="1.0" encoding="UTF-8"?>
<odoo>
      <record id="account_move_view_form" model="ir.ui.view">
        <field name="name">
            account.move.view.form.inherit.portfolio.invoice
        </field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_reference']"
                   position="after">
                <field name="portfolio_purchase_id" invisible="1"/>
                <field name="portfolio_sale_id" invisible="1"/>
                <field name="portfolio_dividend_id" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>