<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_res_groups_extra_form" model="ir.ui.view">
        <field name="name">res.groups.form</field>
        <field name="model">res.groups</field>
        <field name="inherit_id" ref="base.view_groups_form"/>
        <field name="arch" type="xml">
            <field name="share" position="after">
                <field name="notify_users_ids" widget="many2many_tags" domain="[('id', 'in', users)]"
                       options="{'no_create': True, 'no_create_edit': True, 'no_edit': True}"/>
            </field>
        </field>
    </record>
</odoo>